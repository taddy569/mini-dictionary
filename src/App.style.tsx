import { styled } from '@mui/material/styles'

// import Box from '@mui/material/Box'
import Container from '@mui/material/Container'

export const WrapperApp = styled(Container)(() => ({
  border: '1px dotted grey',
}))
